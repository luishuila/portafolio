import { ScrollAbajoDirective } from './scroll-abajo.directive';

describe('ScrollAbajoDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAbajoDirective();
    expect(directive).toBeTruthy();
  });
});
