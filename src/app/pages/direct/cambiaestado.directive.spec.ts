import { CambiaestadoDirective } from './cambiaestado.directive';

describe('CambiaestadoDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiaestadoDirective();
    expect(directive).toBeTruthy();
  });
});
