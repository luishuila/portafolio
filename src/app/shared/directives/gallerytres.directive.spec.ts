import { GallerytresDirective } from './gallerytres.directive';

describe('GallerytresDirective', () => {
  it('should create an instance', () => {
    const directive = new GallerytresDirective();
    expect(directive).toBeTruthy();
  });
});
