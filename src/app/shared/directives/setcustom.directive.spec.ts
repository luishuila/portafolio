import { SetcustomDirective } from './setcustom.directive';

describe('SetcustomDirective', () => {
  it('should create an instance', () => {
    const directive = new SetcustomDirective();
    expect(directive).toBeTruthy();
  });
});
