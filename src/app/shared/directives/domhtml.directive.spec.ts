import { DomhtmlDirective } from './domhtml.directive';

describe('DomhtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new DomhtmlDirective();
    expect(directive).toBeTruthy();
  });
});
