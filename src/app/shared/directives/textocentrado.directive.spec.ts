import { TextocentradoDirective } from './textocentrado.directive';

describe('TextocentradoDirective', () => {
  it('should create an instance', () => {
    const directive = new TextocentradoDirective();
    expect(directive).toBeTruthy();
  });
});
